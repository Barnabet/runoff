// The global Sources ledger moved into the per-project source manager (v1.2b).
// This placeholder keeps the route compiling; Task 6 removes it entirely.
export default function SourcesPage() {
  return (
    <main className="p-8">
      <p>Sources now live inside each project.</p>
    </main>
  );
}
